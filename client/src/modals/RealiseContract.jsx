import React from 'react'
import { useState } from 'react';
import { createVisit } from '../web3/SmartContract.jsx';

let createNewVisit = await createVisit();

const RealiseContract = (setOpenModal, setFile) => {
    return (
        <div className="modalPhotoBackground" >


        </div>
      );
    }

export default RealiseContract